// Define the base endpoint for room-related operations
export const ROOM_ENDPOINT = "/room";

// Endpoint for updating a specific room by its unique ID
export const UPDATE_ROOM_BY_ID_ENDPOINT = "/:room_id";

// Endpoint for updating the status of a specific room by its unique ID
export const UPDATE_ROOM_STATUS_BY_ID_ENDPOINT = "/:room_id/status";

// Endpoint for deleting a specific room by its unique ID
export const DELETE_ROOM_END_POINT = "/:room_id";

// Endpoint for retrieving all rooms
export const ALL_ROOM_ENDPOINT = "";
